spring:
  cloud:
    nacos:
      config:
        server-addr: tl.nacos.com:8848
        file-extension: yml
        shared-dataids: tulingmall-nacos.yml
        refreshable-dataids: tulingmall-nacos.yml
        max-retry: 10
  application:
    name: tulingmall-order
  profiles:
    active: dev

